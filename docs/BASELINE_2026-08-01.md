# HubTube Baseline Report

Date: 2026-08-01  
Status: Completed with declared exclusions  
Mode: Read-only, shallow `main` snapshot

## Source revisions

| Repository | Commit | Files observed | Import edges |
|---|---:|---:|---:|
| `anketci54-coder/tokenoskobi` | `9720d66` | 6,425 | 2,375 |
| `anketci54-coder/quant-simulator` | `6399500` | 13 | 0 |

Full commit identities remain available in the local baseline database and source clones.

## Tokenoskobi

Snapshot hash:
`9f3c9159e1159290daa2b18415803a2d45ea2105b98d956eefd8027136533cbc`

| Classification | Count |
|---|---:|
| Data | 4,712 |
| Documentation | 1,130 |
| Source | 541 |
| Test | 40 |
| Configuration | 2 |

Pending Need Graph proposal:

- `quality.test-coverage` — **MEDIUM**
  - Deterministic evidence: 40 detected test files / 541 source files (`0.0739`).
  - Status: `PENDING`; no automatic activation was performed.

The repository contains a pinned NVIDIA root certificate. It is a public certificate,
not a private key, and is therefore not classified as secret material.

## Quant Simulator

Snapshot hash:
`31d69628047155f8a1a81280ca6b8a3c0a5e52d130a232a852aae8a23ecdb285`

| Classification | Count |
|---|---:|
| Source | 8 |
| Configuration | 3 |
| Documentation | 1 |
| Data | 1 |

Pending Need Graph proposals:

- `dependency.health-monitoring` — **HIGH**
  - Deterministic evidence: `Cargo.lock` is present.
- `quality.test-coverage` — **HIGH**
  - Deterministic evidence: 8 source files and no detected test files.

Both proposals remain `PENDING`; no automatic activation was performed.

### Declared checkout exclusions

Three Git paths cannot be represented in a Windows working tree because `:` is illegal
in Windows filenames:

- `PositionLifecycle::Closed,`
- `PositionLifecycle::Open,`
- `PositionLifecycle::PendingOpen,`

They were excluded from the working-tree baseline. Additional zero-byte, unusual root
paths remain visible in the snapshot and should be reviewed separately. This limitation
must be resolved before the baseline can be marked fully portable.

## Baseline quality corrections

The live baseline exposed and corrected three false-positive sources:

1. Duplicate import statements are now deduplicated before persistence.
2. Initial baseline additions no longer generate fake "recent change" needs.
3. Public PEM certificates are distinguished from private keys.
4. JSON, JSONL, database, tabular, and common image artifacts are classified as data,
   not source code.

## Decision

`BASELINE_SCAN=COMPLETED_WITH_EXCLUSIONS`

No Need Graph proposal was approved automatically. Human approval remains required.
